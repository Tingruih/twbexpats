<section class="m-section-stack" aria-label="數據圖表">
    {% if player_trend_by_year %}
    <div class="chart-wrap glass-panel m-chart-wrap pitcher-trend-wrap">
        <div class="m-filter-bar">
            <span class="filter-bar-title">賽季走勢圖</span>
            <label class="filter-label" for="m-trend-stat-select">數據</label>
            <select id="m-trend-stat-select" class="filter-select">
                {% for key, label in trend_stat_options %}
                <option value="{{ key }}"{% if loop.first %} selected{% endif %}>{{ label }}</option>
                {% endfor %}
            </select>
            <label class="filter-label" for="m-trend-year-select">年度</label>
            <select id="m-trend-year-select" class="filter-select">
                {% for yr in player_trend_by_year %}
                <option value="{{ yr }}"{% if loop.first %} selected{% endif %}>{{ yr }}</option>
                {% endfor %}
            </select>
            <label class="filter-label" for="m-trend-level-select">層級</label>
            <select id="m-trend-level-select" class="filter-select"></select>
        </div>
        <div id="m-trend-chart-legend" class="trend-chart-legend"></div>
        <div id="m-trend-chart-empty" class="trend-chart-empty" role="status" hidden></div>
        <div class="m-performance-chart-container">
            <canvas id="mPerformanceChart"></canvas>
        </div>
    </div>
    {% endif %}

    {% if statcast_available %}
    <div class="chart-wrap glass-panel pitch-plinko-wrap m-chart-wrap">
        <div class="m-filter-bar">
            <label class="filter-label" for="m-plinko-year-select">年度</label>
            <select id="m-plinko-year-select" class="filter-select">
                {% for yr in available_statcast_years %}
                <option value="{{ yr }}"{% if loop.first %} selected{% endif %}>{{ yr }}</option>
                {% endfor %}
            </select>
            <label class="filter-label" for="m-plinko-level-select">層級</label>
            <select id="m-plinko-level-select" class="filter-select"></select>
        </div>

        {% for yr in available_statcast_years %}
        {% set _entries = statcast_by_year[yr] %}
        <div class="m-pitch-plinko-year-container" id="m-plinko-{{ yr }}" {% if not loop.first %}style="display:none;"{% endif %}>
            {# data-chart-key matches the #chart-data-{kind}-{{ yr }}-{{ loop.index0 }}
               ids rendered once by partials/chart_data.j2. #}
            {% for _e in _entries %}
            {% set _level = _e.sport_level %}
            <div class="pitch-plinko-level-container m-pitch-plinko-level-container" data-level="{{ _level | level_display(yr) }}" data-level-label="{% if _level == '_combined' %}All Levels{% else %}{{ _level | level_display(yr) }}{% endif %}" data-chart-key="{{ yr }}-{{ loop.index0 }}" {% if not loop.first %}style="display:none;"{% endif %}>
                {% if is_pitcher %}
                <div class="pitch-chart-grid m-pitch-chart-grid">
                    <section class="pitch-chart-section"><div class="pitch-usage-hand-root"></div></section>
                    <section class="pitch-chart-section"><div class="pitch-movement-root"></div></section>
                </div>
                {% endif %}
                <div class="pitch-plinko-root"></div>
            </div>
            {% endfor %}
        </div>
        {% endfor %}
    </div>
    {% elif not player_trend_by_year %}
    <div class="glass-panel empty-season-msg">尚無本賽季數據圖表</div>
    {% endif %}
</section>

"""Generic utilities with no baseball knowledge.

Submodules:
    json     — tolerant JSON load/dump helpers
    numbers  — safe conversions, ratio/mean helpers
    dates    — date parsing and the Taiwan timezone
    units    — height/weight display conversions
    obj      — attribute-access dict used as the template data carrier
"""
